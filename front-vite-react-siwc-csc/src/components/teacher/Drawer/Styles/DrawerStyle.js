import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  navLink: {
    display: "block",
    margin: "0.5rem 0",
    textDecoration: "none",
  },
}));

export default useStyles;
